# 数字造机 Web 原型交接文档

更新时间：2026-07-30

项目目录：`/Users/mac/Downloads/原型APP/Custom Dashboard Management`

当前分支：`master`

最近提交：`db0f474 chore: 更新API地址为localhost:10882，更新guidelines结构，修改组件样式`

> 本工作区存在大量未提交修改和新增文件。它们包含用户持续确认的 UI、交互和设计规范成果，不要执行 `git reset --hard`、`git checkout -- .` 或批量覆盖。

## 1. 任务目标

这是“墨影工作台 / 数字造机”的高保真可交互 Web 原型。当前目标是：

1. 保持软件产品、装机记录、型号模板、版本管理、数据管理等模块的视觉与交互一致。
2. 使用项目内部 HeroUI 风格封装和 Design Token，不直接照搬用户提供的低保真原型样式。
3. 将“数据管理”建设为与软件产品、装机记录同级的主导航模块，二级功能包括：
   - 字段字典
   - 构型模板
   - 参数定义
4. 所有业务设计必须符合真实功能逻辑，而不仅是静态页面：
   - 字典字段、取值、级联关系可维护；
   - 构型模板能生成运动链槽位并限制模块规格；
   - 参数定义能按真实组件层级维护字段；
   - 内置数据和自定义数据遵循不同编辑/删除权限。
5. 逐步沉淀全局视觉规范：颜色、字号、圆角、间距、表格、弹窗、抽屉、页面模板、响应式和状态规则。

目前仍是前端原型，数据主要存放在 React 状态和 `localStorage`，没有真实后端、数据库或 ROS 服务。

## 2. 用户已经确认的设计原则

后续修改必须优先遵守：

- 用户提供的截图主要表达信息架构和业务逻辑，不是像素级照抄目标。
- UI 应符合现有 HeroUI 风格封装与产品 Design Token。
- 页面边距、卡片圆角、按钮高度、表格行高需要全局一致。
- 列表页应参考“软件产品”和“装机记录”的页面骨架。
- 表格只保留横向分隔线，不要竖线。
- 搜索与新增按钮放在同一工具栏。
- 只有选中项显示蓝色选中背景，不要让所有树节点都带蓝色条。
- 数据较多时优先使用主从布局、抽屉和内部滚动，不要堆叠很多大卡片。
- 固定分类应使用稳定的导航结构，不要伪装成可新增的动态分类。
- 工具型操作使用“刷新 / 导出”同类白底描边按钮；创建操作使用主按钮。
- 弹窗标题区域避免冗余图标和大段说明。
- 内置名称可调整、标识符不可修改、内置字段及取值不可删除；自定义内容支持完整 CRUD。

## 3. 已完成内容

### 3.1 全局设计体系

- 主色、字号、间距、圆角、阴影、按钮和表格规范已经写入设计规范目录。
- 内部 UI 统一通过 `ProductUI.tsx` / `ArcoLike.tsx` 使用，新增业务组件不要直接随意引入第三方原子组件。
- 设计规范版本当前为 `v1.5.0`。
- `npm run check` 会执行规范校验和 Vite 生产构建。
- 当前约定的核心尺寸：
  - 页面主标题：20px；
  - 正文：14px；
  - 辅助文字：12px；
  - 常用按钮/输入框：40px；
  - 表格表头：44px；
  - 表格数据行：60px；
  - 卡片圆角：16px；
  - 控件/按钮圆角：8px。

主要文件：

- `guidelines/tokens/design-tokens.json`
- `guidelines/components/component-specs.json`
- `guidelines/docs/ui-guidelines.md`
- `scripts/check-design-system.mjs`
- `src/app/components/ProductUI.tsx`
- `src/app/components/ArcoLike.tsx`
- `src/styles/design-system.css`
- `src/styles/arco-like.css`
- `src/styles/style-presets.css`

### 3.2 主导航和数据管理入口

- “数据管理”与软件产品、装机记录同级。
- 点击/悬停数据管理后显示浮层二级导航。
- 二级导航已按用户最后要求精简为纯文字：
  - 字段字典
  - 构型模板
  - 参数定义
- 已移除二级浮层中的“数据管理”标题、图标、描述和右侧箭头。
- 当前二级页面通过事件和 `localStorage` 切换，避免再增加一整列持久二级导航。

关键实现：

- `src/app/App.tsx`
- `src/app/components/DataManagementManager.tsx`
- `src/styles/business/data-management.css`

事件与存储：

- 当前二级页面：`digital-machine-data-management-section`
- 切换事件：`digital-machine:data-management-section`

### 3.3 字段字典

数据模型位于 `src/app/dictionaryData.ts`，包含：

- 分类；
- 字段；
- 枚举取值；
- 字段 `seq`；
- 启用状态；
- 内置/自定义来源；
- 级联规则。

当前预置分类：

- 型号模板分类；
- 组件库分类；
- 项目扩展属性。

组件库分类预置字段：

- 组件类型；
- 子类型；
- 规格。

已完成的交互：

- 左侧为“分类 → 字段”两级树，只保留一条层级引导线。
- 字段按 `seq` 排序。
- 右侧顶部显示分类、字段、标识符、字段说明和明确的“已启用/已停用”状态。
- “枚举取值 / 级联配置”使用分段 Tab。
- Tab、搜索/父字段信息、新增按钮保持同一行。
- 枚举值支持搜索、新增、编辑、启停和删除。
- 内置枚举值可编辑，但标识符只读、删除按钮禁用。
- 自定义枚举值可删除，并同步清理引用它的级联规则。
- 级联配置限定为大 `seq` 字段依赖小 `seq` 字段。
- 数据管理下的字典页与原有“字典配置”页共用 `dictionaryCategories` 数据源。

存储：

- `digital-machine-dictionary-config-v1`

主要文件：

- `src/app/dictionaryData.ts`
- `src/app/components/DataManagementManager.tsx`
- `src/app/components/DictionaryConfigManager.tsx`
- `src/styles/business/data-management.css`
- `src/styles/business/dictionary-config.css`

### 3.4 构型模板

页面入口：`数据管理 → 构型模板`。

已完成：

- 构型列表、搜索、新增、编辑、删除、启停。
- 构型字段：名称、标识符、自由度、启用状态、描述。
- 自由度支持 2～12 轴。
- 根据自由度生成：
  - `BASE`
  - `J1/L1`
  - `J2/L2`
  - 依此类推。
- 列表中的“装配模板”打开具体构型抽屉。
- 顶部“槽位规则”按钮放在“新增构型”旁边，并与“刷新 / 导出”使用同类白底描边样式。
- 槽位规则是全局配置，不属于某一个具体构型。
- 全局槽位规则按底座、关节、连杆维护允许规格，支持同类型全选。
- 规格来源于“字段字典 → 组件库分类 → 规格”。
- 系统通过字段字典的“组件类型 → 子类型 → 规格”级联生成默认规则。
- 装配模板逐槽位配置允许规格，并受全局槽位规则约束。
- 某个具体槽位不选择规格时，表示继承对应类型的全局槽位规则。
- 修改全局规则后，会清理装配模板中超出规则范围的显式规格。
- 支持恢复默认规则、恢复继承状态和保存。
- 旧的构型本地数据会迁移到全局槽位规则结构。

存储：

- 构型：`digital-machine-configuration-templates`
- 全局槽位规则：`digital-machine-global-slot-rules`

主要文件：

- `src/app/components/ConfigurationTemplateManager.tsx`
- `src/styles/business/data-management.css`

### 3.5 参数定义

页面入口：`数据管理 → 参数定义`。

当前采用左右主从结构：

- 左侧：固定组件分类和字段数量；
- 右侧：当前实际参数归属的字段表；
- 顶部：分类/字段搜索和新增字段。

固定一级分类：

- 底盘；
- 机械臂整臂；
- 机械臂模块；
- 升降机构。

“机械臂模块”包含固定二级分类：

- 底座：`arm_module:base`
- 关节：`arm_module:joint`
- 连杆：`arm_module:link`
- 末端：`arm_module:end_effector`

业务规则：

- “机械臂模块”父级只汇总和展开，不直接承载参数字段。
- 新增/编辑字段只能选择末级组件归属。
- 原先直接挂在机械臂模块上的字段会自动迁移到“关节”。
- 父级数量为四个子模块字段数之和。
- 搜索支持一级分类、二级分类、字段名称和标识符。

字段功能：

- 名称；
- 唯一标识符；
- 所属组件；
- 数据类型；
- 启用状态；
- 默认值；
- 新增、编辑、删除、启停。

数据类型：

- 枚举型；
- 整型；
- 浮点型；
- 文本型。

枚举型支持：

- 枚举项显示名称；
- 枚举项标识符；
- 新增/删除枚举项；
- 枚举项标识符重复校验；
- 默认枚举值选择。

同步机制：

- 存储：`digital-machine-parameter-definitions`
- 更新事件：`digital-machine:parameter-definitions-updated`
- 导出读取函数：`readParameterDefinitions()`

目前还没有实际组件参数表单消费该事件，详见“当前问题”。

主要文件：

- `src/app/components/ParameterDefinitionManager.tsx`
- `src/styles/business/data-management.css`

### 3.6 数据管理表格规范

数据管理中的以下表格已统一为软件产品/装机记录样式：

- 字段字典枚举值表；
- 字段字典级联表；
- 构型模板列表；
- 参数定义字段表。

共享类：

- `.data-management-table`
- `.data-management-table--actions`

统一行为：

- 44px 浅灰表头；
- 60px 数据行；
- 16px 水平单元格内边距；
- 仅横向分隔线；
- 无竖线；
- Sticky 表头；
- 行 Hover；
- 操作列右对齐；
- 内容区域内部滚动。

不要再分别给三张表增加互相冲突的表头/行高样式。

### 3.7 型号模板与型号库

本工作区已有较多型号模块改动，主要集中在 `RobotModelManager.tsx`：

- 型号库改为 HeroUI 风格卡片视图，卡片包含表格信息和 3D 预览。
- 型号详情重新整理为左侧信息/软件配置、中间 3D、右侧模型结构/节点参数。
- 顶部冗余信息和面包屑按用户标注精简。
- 软件版本树参考版本管理的产品分类树。
- 只有选中节点显示蓝色背景。
- 模型结构节点支持拖拽调整层级。
- 节点 Hover 操作逻辑、增删改入口和模型结构面板有多轮调整。
- 型号详情中的汇总数据块按用户要求移除。
- 型号库搜索和新增保持同一工具栏。

注意：这里有大量未提交改动，后续接手前应先实际运行确认视觉状态，不要从旧提交重新覆盖。

### 3.8 版本管理与产品分类

`ProductVersionManager.tsx` 已有多轮交互调整：

- 产品类型 → 子品类 → 产品三级目录。
- 树样式被其他模块作为参考规范。
- 大类、子品类、产品 Hover/选中时显示三点菜单。
- 菜单支持新增、编辑、删除。
- 菜单样式使用项目内部 HeroUI 风格，不使用浏览器默认菜单。
- 产品搜索与新增位于同一行。

### 3.9 装机记录与软件产品

这两个页面是当前列表页和表格样式基准：

- `SoftwareManager.tsx`
- `InstallationRecordsManager.tsx`

装机记录：

- 搜索、刷新、导出；
- 首尾列固定；
- 无竖线；
- 详情弹窗只保留关闭按钮，不保留编辑按钮；
- 详情中展示软件安装状态和记录。

后续数据管理表格如果出现样式偏差，优先对照这两个文件，而不是自行创建新的表格视觉。

### 3.10 自定义首页与编辑器

已完成的主要方向：

- 编辑器按用户参考图重新组织顶部、左侧组件库、中央画布和右侧属性面板。
- 组件支持拖拽、移动、缩放、删除和属性配置。
- 首页组件库与机器人 3D 组件库是两个不同概念。
- `components` 仅表示首页编辑组件面板。
- `robotComponents` 表示机器人 3D 组件库。

## 4. 当前问题

### 4.1 高优先级

1. **当前工作区未提交。**
   - 分支仍是 `master`。
   - `git status` 有大量修改和新增文件。
   - 不要假设某个功能已经存在于 Git 历史中。
   - 首次接手应先运行 `git status --short`，再打开当前工作区文件。

2. **用户要求“组件库、外设库、用户管理模块暂时隐藏”，但当前主导航仍显示“组件库”。**
   - `App.tsx` 中 `robotComponents` 仍在 `navItems`。
   - 外设库和用户管理已隐藏。
   - 下一会话应先向用户确认这里的“组件库”是否仍需隐藏，再修改，避免误伤首页编辑器的 `components`。

3. **数据管理仍保留两套字典入口。**
   - 旧入口：主导航“字典配置” → `DictionaryConfigManager`。
   - 新入口：数据管理 → 字段字典 → `FieldDictionaryVersionView`。
   - 两者共用数据，但 UI 不同。
   - 新入口是用户最近持续调整的版本；是否删除旧入口尚未得到明确确认。

4. **参数定义的“同步组件表单”目前只有数据源和事件，没有实际消费方。**
   - 已提供 `readParameterDefinitions()`。
   - 已广播 `digital-machine:parameter-definitions-updated`。
   - 组件库/组件详情参数表单尚未读取这些定义。

5. **所有新增业务数据仍是本地原型数据。**
   - 字典、构型、槽位规则、参数定义使用 `localStorage`。
   - 没有后端并发、权限、服务端校验、版本迁移框架或 API 错误处理。

### 4.2 中优先级

6. **需要一次完整人工视觉回归。**
   建议至少检查：
   - 1440×900；
   - 1920×1080；
   - 1024px 宽；
   - 768px 以下。

   重点查看：
   - 数据管理二级菜单；
   - 字段字典左右结构；
   - 构型模板抽屉；
   - 参数定义两级左侧树；
   - 表格横向滚动和操作列；
   - 长中文名称和长标识符省略。

7. **构型槽位规则的字典归属映射仍是原型逻辑。**
   - 底座映射到组件类型 `chassis`。
   - 关节和连杆映射到 `robot_arm`。
   - 通过类型 → 子类型 → 规格级联求默认规格。
   - 如果某条链路没有规格级联，会回退为全部已启用规格。
   - 后续如果字段字典补充更精细的“模块归属”，需要替换此推断。

8. **参数分类迁移是一次性启发式迁移。**
   - 检测到旧数据没有四个机械臂子模块时：
     - 新建底座/关节/连杆/末端；
     - 旧“机械臂模块”字段迁移到“关节”。
   - 正式产品应使用带版本号的数据迁移，而不是运行时启发式判断。

9. **Vite 构建存在大 Chunk 警告。**
   - 当前构建成功。
   - JS 主包约 1.17 MB，gzip 约 335 KB。
   - 稳定功能前不要为了该告警进行大规模无关重构。

10. **没有本地 TypeScript CLI。**
    - `package.json` 没有 `typescript` devDependency。
    - 执行 `npx tsc --noEmit` 会尝试访问 npm registry；当前环境网络受限时会失败。
    - 当前可靠验证方式是 `npm run check`。

## 5. 下一步计划

建议按以下顺序继续：

1. 让用户对最新“参数定义两级左侧结构”做视觉确认。
2. 完成数据管理三页的人工浏览器回归，尤其是滚动、长文本、空状态和窄屏。
3. 根据用户确认处理旧“字典配置”入口是否保留。
4. 确认主导航“组件库”是否要按早先要求隐藏。
5. 将 `ParameterDefinitionManager` 接入实际组件参数配置表单：
   - 根据组件类型读取对应末级分类；
   - 仅加载启用字段；
   - 枚举型渲染下拉选择；
   - 数值型进行数值校验；
   - 默认值填充；
   - 监听参数定义更新事件。
6. 将字段字典、构型模板、槽位规则和参数定义迁移到正式 API。
7. 增加正式数据迁移版本号和服务端唯一性校验。
8. 功能稳定后再做代码拆分：
   - 把 `FieldDictionaryVersionView` 从 `DataManagementManager.tsx` 拆出；
   - 抽取共享 `StatusSwitch`；
   - 抽取共享数据管理表格组件；
   - 清理已无入口的旧样式和不可达占位分支。
9. 最后再处理 Vite 分包告警和自动化 UI 测试。

## 6. 踩过的坑

### 6.1 不要把原型 UI 原样搬过来

用户多次指出原型只是结构参考。正确做法是：

- 保留信息架构和业务逻辑；
- 使用现有 HeroUI 风格组件；
- 对大量内容采用主从布局、抽屉和内部滚动；
- 避免重复表头、巨型卡片和多余分栏。

### 6.2 固定分类与动态分类不能混用

参数定义最初做成四张可展开大卡片，导致：

- 重复表头；
- 内容被截断；
- 页面纵向浪费；
- 固定分类看起来像动态列表。

最终改为左右主从结构。机械臂模块又进一步改为父级 + 四个固定子模块。

### 6.3 槽位规则与装配模板不是一回事

最初把两层配置混在同一个构型抽屉里。实际逻辑应为：

- 全局槽位规则：按底座/关节/连杆限定规格；
- 具体装配模板：按 BASE、J1/L1…选择规格并继承全局规则。

因此“槽位规则”现在位于顶部，与“新增构型”相邻；构型行内只进入“装配模板”。

### 6.4 “全部规格”与“不选择”的语义必须明确

- 全局槽位规则不选具体规格：允许该类型下全部规格。
- 具体装配模板不选具体规格：继承全局槽位规则。

不要把两种空数组语义混成同一提示。

### 6.5 字典树不能套错层级

字段字典只有“分类 → 字段”两级。最初复用了版本管理三级树的两层引导线，导致双竖线和过深缩进。

正确做法：

- 只使用一层 `taxonomy-tree-category-children`；
- 不再嵌套 `taxonomy-tree-brand-list`。

### 6.6 蓝色背景只表示选中

产品分类树和字典树中，蓝色条只能出现在当前选中项。不要为了“统一样式”给所有叶子节点加蓝底。

### 6.7 数据管理二级导航不要常驻一整列

用户明确反对为字段字典/构型模板/参数定义单独占一列导航。当前采用主导航旁的浮层菜单，并最终精简为纯文字。

### 6.8 表格不要各写一套

数据管理三页最初分别定义表头、行高和边框，视觉上与软件产品、装机记录不一致。现在通过：

- `.data-management-table`
- `.data-management-table--actions`

统一。后续不要再在页面级样式里覆盖成不同高度或添加竖线。

### 6.9 首页组件库与机器人组件库不能共用状态 key

- `components`：首页编辑器的组件面板。
- `robotComponents`：机器人 3D 组件库。

曾经混用导致点击“编辑面板”跳到 3D 组件库。后续隐藏/恢复导航时尤其小心。

### 6.10 Portal 不继承局部 CSS 变量

HeroUI Modal/Drawer 使用 Portal。如果主题变量只写在页面局部容器，弹窗可能透明或颜色错误。机器人模块曾通过把局部主题变量同步到 `document.documentElement` 修复。

新增 Portal 类组件时应确认主题 Token 在 Portal 根节点可用。

### 6.11 不要直接假设 `npx tsc` 可用

项目没有本地 TypeScript CLI。`npx tsc --noEmit` 会尝试下载名为 `tsc` 的包，在受限网络环境下出现 `ENOTFOUND`。不要把它作为当前交付门禁，使用 `npm run check`。

### 6.12 机械臂整臂组件创建流程

`组件库` 已新增“创建机械臂整臂组件”两步弹窗：

- 组件库采用与型号库一致的两级结构：第一层为组件卡片列表，第二层为单个组件详情；搜索和“新增组件”只能放在第一层，详情通过左上角返回按钮回到列表；
- 基础信息与构型：组件名称、唯一英文标识符、组件类型、级联子类型、启用中的构型模板；
- 模块装配：按构型模板生成底座、J/L 运动链槽位，读取全局槽位规则或模板覆盖规格，并要求每个槽位选择匹配模块；
- 支持部分模块预览；
- 提交后数据写入 `digital-machine-arm-components`，当前详情切换为新组件，并以英文标识符生成 Link/Joint 名称前缀；
- 标识符校验规则为 `^[A-Za-z][A-Za-z0-9_]*$`，与已有组件重复时禁止继续。

构型模板页面为此开放了只读共享接口 `readConfigurationTemplates`、`readGlobalSlotPolicies` 及相关类型，后续不要在组件库重复解析相同 localStorage 数据。

## 7. 快速开始与验证

```bash
cd "/Users/mac/Downloads/原型APP/Custom Dashboard Management"
npm run dev
```

完整交付检查：

```bash
npm run check
git diff --check
```

最近一次检查结果：

- 设计规范 `v1.5.0` 校验通过；
- Vite 生产构建通过；
- `git diff --check` 通过；
- 仅存在大 Chunk 警告。

## 8. 关键文件索引

| 文件 | 作用 |
| --- | --- |
| `src/app/App.tsx` | 顶层状态、主导航、模块路由、字典数据持久化。 |
| `src/app/components/DataManagementManager.tsx` | 数据管理二级页面切换及当前字段字典新版页面。 |
| `src/app/components/DictionaryConfigManager.tsx` | 旧“字典配置”完整页面。 |
| `src/app/dictionaryData.ts` | 字段字典类型、预置数据、级联读取函数。 |
| `src/app/components/ConfigurationTemplateManager.tsx` | 构型 CRUD、全局槽位规则、具体装配模板。 |
| `src/app/components/ParameterDefinitionManager.tsx` | 参数定义、两级组件分类、枚举项维护、同步事件。 |
| `src/styles/business/data-management.css` | 数据管理三页布局、表格和抽屉样式。 |
| `src/styles/business/dictionary-config.css` | 旧字典配置页样式。 |
| `src/app/components/SoftwareManager.tsx` | 标准列表页/表格视觉参考。 |
| `src/app/components/InstallationRecordsManager.tsx` | 标准表格、固定首尾列、工具按钮参考。 |
| `src/app/components/ProductVersionManager.tsx` | 产品分类树和版本管理交互参考。 |
| `src/app/components/RobotModelManager.tsx` | 型号库、型号详情、3D、结构树、机器人组件库。 |
| `src/app/components/ProductUI.tsx` | 产品 UI 统一导出入口。 |
| `src/app/components/ArcoLike.tsx` | HeroUI 风格兼容封装。 |
| `guidelines/docs/ui-guidelines.md` | 视觉和交互规范。 |
| `guidelines/tokens/design-tokens.json` | Design Token。 |

## 9. 给新会话的工作方式建议

1. 先读本文件，再读用户最新截图和最后一条反馈。
2. 修改前先运行 `git status --short`，确认不要覆盖未提交成果。
3. UI 任务先对照现有软件产品、装机记录、产品分类树和 Design Token。
4. 先确认业务层级，再决定页面布局；不要仅凭截图猜交互。
5. 修改后执行 `npm run check && git diff --check`。
6. 向用户汇报时只说实际完成内容，不声称已接后端或完成真实 3D/ROS 能力。

---

# 组件库2与机器人编辑器交接文档

更新时间：2026-08-14
项目目录：`/Users/mac/Downloads/原型APP/Custom Dashboard Management`

> 当前工作区存在大量未提交修改和未跟踪资源。接手后先执行 `git status --short`，不要使用 `git reset --hard`、`git checkout -- .`、批量覆盖或删除未跟踪目录。

## 1. 任务目标

本阶段目标是在现有“数字造机”后台中新增“组件库2”，并保持型号模板、组件库、组件库2与既有设计规范一致。

组件库2需要提供基于 URDF + GLB/STL 的机械臂编辑器，核心能力包括：

1. 导入 URDF 及多个网格资源，并恢复 Link / Joint / Mount 层级。
2. 结构树查看、选中、显隐、锁定、新增、删除和层级拖动。
3. 素材库上传、删除、预览、添加到结构树，以及拖入 Link。
4. 关节角度实时调试、单位切换、范围显示、重置及三维联动。
5. Link 的 Visual、Collision、Inertial 属性编辑。
6. Mesh 缩放、位置、欧拉角/弧度/四元数旋转和材质配置。
7. Collision 多对象管理、Mesh 选择/上传、独立显隐与场景高亮。
8. Mass、质心、惯性张量、密度、主惯量和主轴矩阵配置。
9. 三维旋转、平移、缩放、拾取、高亮、变换箭头、原点和视角转换。
10. 配置保存、URDF 映射、重新打开后回显，以及导入/导出。
11. 列表页与既有后台统一；进入详情/编辑后使用与首页自定义相同的全屏工作区。

## 2. 已完成内容

### 2.1 组件库2与真实模型

- 已新增 `RobotComponentLibrary2` 页面和入口。
- 已接入用户提供的 Marvin URDF/STL/GLB 资源作为真实展示模型。
- 已增加独立的“新建组件”空白流程，不再强制用完整示例作为新项目初始状态。
- 已建立素材库、模型结构、关节控制、三维场景和右侧属性面板布局。
- 已支持素材上传、删除、预览、添加到 Link，以及从素材库拖入结构树。
- 已支持结构节点删除、拖动调整层级、选中、显隐和锁定。
- 已接入三维模型查看、模型拾取、局部高亮、网格显示、视角切换和视角重置。
- 已增加选中对象信息、世界坐标、包围尺寸和视角坐标 Gizmo。
- 已实现关节滑杆、rad/° 单位切换、数值输入、归零和模型联动。
- 已建立 Visual、Collision、Physical 三个属性页签。
- 已实现 Collision/Inertial 的主要数据结构和 URDF 字段映射。
- 已实现导入/导出合并菜单、保存配置和退出编辑。
- 已加入 gRPC-Web 关节状态接口面板和基础同步逻辑；仍属于原型集成。

主要文件：

- `src/app/components/RobotComponentLibrary2.tsx`
- `src/app/components/MarvinUrdfViewer.tsx`
- `src/app/lib/grpcArmJoints.ts`
- `src/styles/business/robot-component-library-2.css`
- `public/models/`
- `Marvin M6-S-CCS-696-V4.0_Base_and_Stand_Asm urdf/`
- `urdf-html-1.0-main/`

### 2.2 三个机器人库的公共外层

- 新增 `src/styles/business/robot-library-shared.css`。
- 型号模板、组件库、组件库2共用列表标题、工具栏、搜索、内容区和卡片网格样式。
- 工业模式已移除错误的白色标题条和底边线。
- 工业列表标题正在对齐字典配置、软件产品、装机记录的标准：
  - 标题区 `padding: 12px 16px 0`；
  - 标题与内容间距 12px；
  - 内容区 `padding: 0 16px 16px`；
  - 标题与操作区底部对齐；
  - 副标题 12px。
- 已修正组件库列表被详情页工业 `12px` padding 误命中的问题。
- 已移除组件库2标题、副标题写死的 `30px / 22px` 高度。
- 已去掉素材库和模型结构标题旁的 20/39 数量。

### 2.3 全屏详情/编辑

- 组件库2编辑状态已隐藏左侧导航和全局顶栏。
- 型号模板与组件库新增 `onDetailChange` 状态回传。
- 点击型号/组件进入详情时，`AppShell` 会折叠导航并隐藏全局顶栏。
- 型号模板与组件库详情已增加自身工作区顶栏和返回入口。
- 返回列表后恢复后台导航框架。

主要文件：

- `src/app/App.tsx`
- `src/app/components/RobotModelManager.tsx`
- `src/styles/business/robot-library-shared.css`

### 2.4 UI 规范修正

- 素材库、结构树、关节控制改为独立卡片，不把关节控制塞进结构树内部。
- 搜索与新增按钮放在标题操作区，并使用统一 IconButton 规则。
- 素材库标题移除无意义图标、默认选中和“应用”文案。
- 去掉“单击预览 · 双击应用到当前属性”提示。
- 统一普通文本为 `#333` 语义色，选中项使用主题色。
- 统一素材列表、树节点、Mesh 列表的选中态语义。
- 移除占位工具条、难以理解的碰撞标注、操作说明条和重复对象信息。
- 避免全模型染色；选中仅影响当前 Link/Joint，并保留原材质。
- 右侧属性面板保留 Visual / Collision / Physical，不重复展示 Joint 信息卡。
- 多处标题分隔线改为规范要求的卡片内缩分隔线；不在不需要的区域添加通栏横线。
- 图标按钮统一为规范尺寸、背景、圆角和交互状态。

## 3. 用户纠正项复盘

归因说明：

- **信息不足**：原始需求没有给出足以确定唯一实现的字段、资源或视觉基准，后续补充后才能准确落地。
- **判断逻辑问题**：项目内已有规范、现成页面或参考模块，仍凭经验新增样式、误读层级、局部打补丁或未做完整验证。
- **混合**：既有补充信息，也存在未先检查现有实现或未选择稳妥默认值的问题。

| 修改内容 | 错误归因 | 下次指令建议 |
|---|---|---|
| 组件库2必须是新建入口，列表外观沿用旧组件库，编辑页单独变化 | 判断逻辑问题 | “新增页面必须复用现有同类列表，不允许复制后自行改一套外层样式。” |
| 编辑页应像首页自定义一样全屏，隐藏后台导航和全局顶栏 | 信息不足（最初仅明确组件库2）；后续扩展到三个库 | “所有卡片详情/编辑统一进入全屏工作区，复用首页自定义的 AppShell 折叠机制。” |
| 模型结构必须参考现有设备结构树 | 判断逻辑问题 | “开始编码前先定位并复用设备结构树组件及其 spacing/selected/action 规则。” |
| 页面四周出现不应有的空隙 | 判断逻辑问题 | “全屏编辑根容器必须 `inset:0`/100%，禁止额外页面 gutter；先检查父级 AppShell。” |
| 添加了规范中不存在的工具切换 | 判断逻辑问题 | “禁止新增无法从设计规范或参考稿中找到的交互控件；不确定先不加。” |
| 属性 Tab 样式错误，双侧栏样式不统一 | 判断逻辑问题 | “Tab、左右面板必须直接使用 ProductUI/Hero UI 现有组件与 Token，禁止手写近似样式。” |
| 缺少素材库列表 | 信息不足偏少；原需求已提到多个 Mesh，仍应推导资源管理入口 | “编辑器固定包含素材库、结构树、场景、属性、关节控制五个区域，不得遗漏。” |
| 结构树可读性差，显隐/锁定图标长期占位 | 判断逻辑问题 | “树节点只常驻名称与类型；低频操作收进更多菜单，状态仅异常时展示。” |
| Link 缺少完整 Collision 与 Inertial 配置 | 信息不足 | “按 URDF 字段清单逐项实现 collision、origin、mesh scale、mass、inertia 和派生参数，不得只做占位 Tab。” |
| Collision 多对象、新增删除切换、显隐、Mesh 选择/上传 | 信息不足 | “Collision 采用一对多列表模型；单击预览、双击应用、当前选择必须明确显示。” |
| 惯性参考系、质心、张量、密度、主惯量、主轴矩阵 | 信息不足 | “Physical 必须覆盖 origin、mass、COM、6 项 inertia、density、I1-I3、3×3 主轴矩阵。” |
| 应用用户提供的 Marvin 真实模型 | 信息不足 | “优先使用指定资源目录中的真实 URDF/网格，不得用示意几何替代验收效果。” |
| 应参考 `urdf-html-1.0-main` 的编辑器与接口 | 信息不足偏少；提供后未第一时间完整复用 | “先审计参考模块的解析、渲染、选中和接口能力，再决定复用/迁移清单。” |
| 中间编辑器渲染粗糙 | 判断逻辑问题 | “三维效果必须以参考模块实模为基线，禁止用低保真 SVG/占位模型充当最终展示。” |
| 工具图标区域含义不明 | 判断逻辑问题 | “所有图标必须有明确任务、Tooltip 和可验证状态；无法说明用途的控件删除。” |
| 选中 Joint2_L 后应展示 Joint2_L 的信息 | 判断逻辑问题 | “选择状态由结构树、关节控制、3D 拾取共享同一 selectedNode，属性面板只读该对象。” |
| 模型出现螺旋纹/摩尔纹 | 判断逻辑问题 | “真实网格先检查材质、法线、深度冲突、重复表面和抗锯齿；不得只调整颜色掩盖。” |
| 选择关节应局部变色，不应使用大包围盒框住整段 | 判断逻辑问题 | “选中逻辑复用参考模块：局部材质高亮 + 变换 Gizmo，不使用粗大包围盒作为主选中态。” |
| Joint 信息卡与 Visual/Collision/Physical 重复 | 判断逻辑问题 | “右侧只保留三类属性 Tab；Joint 选择仅切换当前对象和关节控制高亮，不新增重复面板。” |
| 碰撞体大色块、质心标注和操作说明条含义不清 | 判断逻辑问题 | “辅助信息默认克制显示，必须有图例/开关；无法理解的 HUD 和教学条删除。” |
| 选中后整个机器人变成“小蓝人” | 判断逻辑问题 | “高亮只能克隆/覆盖命中的对象材质，离开选择必须恢复原材质，禁止遍历整场景统一染色。” |
| 右侧重复展示两次标题 | 判断逻辑问题 | “同一层级只允许一个主标题；对象摘要不能再次复述面板标题。” |
| 世界坐标和包围尺寸应放到 3D 左下角 | 信息不足（具体位置由参考图补充） | “对象空间信息放进场景 HUD，不占用属性表单；HUD 位置按参考稿。” |
| 整体样式必须使用 Hero UI / 项目现有组件 | 判断逻辑问题 | “所有 Button/Input/Tab/Card/Modal 先从 ProductUI 查找；不得手写第二套组件皮肤。” |
| 属性卡顶部多余对象标题需要去掉 | 判断逻辑问题 | “页面顶栏已有对象名时，属性卡不再重复标题，仅保留 Tab 和字段。” |
| 搜索应放在新增旁边 | 判断逻辑问题 | “列表页统一结构：左侧 title/description，右侧 search + primary action。” |
| 素材库标题不应带装饰图标 | 判断逻辑问题 | “标题只保留文本；图标仅用于可执行操作或明确类型识别。” |
| “应用”含义不清，预览/应用提示应删除 | 判断逻辑问题 | “按钮文案必须描述结果；可通过直接操作理解的提示不要长期占位。” |
| 关节控制样式不合适，需要参考指定卡片 | 信息不足（精确视觉参考后补充）+ 判断逻辑问题（不应自行发明） | “关节控制严格按参考卡片：独立面板、行式滑杆、数值框、单位切换和重置。” |
| 搜索框出现浅蓝底，不符合规范 | 判断逻辑问题 | “搜索使用标准 Input 默认表面色；聚焦才显示主题边框/Focus Ring。” |
| 同一页面存在两种选中样式 | 判断逻辑问题 | “先定义唯一 selected token，再让素材、树、Mesh 和 Joint 复用；禁止局部自定义颜色。” |
| 关节控制被误塞进模型结构内部 | 判断逻辑问题（误读“下面”为嵌套） | “关节控制是结构树下方的独立卡片，不是结构树 children；布局层级必须先画 DOM 树确认。” |
| 可视化 Tab 原本符合规范，不应被错误重做 | 判断逻辑问题 | “用户明确确认过的组件列为冻结基线，后续修改不得顺带重构。” |
| 搜索按钮与新增按钮规则不一致 | 判断逻辑问题 | “同一标题操作区的 icon button/primary button 必须来自同一尺寸、圆角和状态规范。” |
| 字体大小不一致、编辑框存在两套样式 | 判断逻辑问题 | “先做 typography/input inventory，消除局部 font-size、height、background 硬编码。” |
| 输入框样式不在设计规范中 | 判断逻辑问题 | “禁止为机器人页面创建专用输入皮肤；只允许 ProductTextInput/ArcoTextInput 规范变体。” |
| 未选中文本应为 `#333`，选中文本用主题色 | 信息不足（明确颜色后补充） | “普通文本统一 `--ds-color-text`，选中文本统一 `--ds-color-accent-text`，不要在组件内猜色。” |
| 缺少 3D 视角转换 Gizmo | 信息不足 | “3D 场景必须包含右下角 XYZ 视角 Gizmo，并支持正交视角切换。” |
| 实时关节接口浮层样式突兀 | 判断逻辑问题 | “接口配置使用标准 Popover/Panel 表单密度，与场景 HUD 分层，不得自造黑框表单。” |
| 素材库 20、模型结构 39 数量应去掉 | 信息不足（明确偏好后补充） | “标题默认不展示总数，除非数量影响决策或设计稿明确要求。” |
| 导入/导出应合成一个按钮，关联 Mesh 去掉 | 信息不足 | “顶栏动作按任务合并为 Import/Export 菜单；不保留与素材库能力重复的入口。” |
| 退出编辑样式与首页自定义不一致 | 判断逻辑问题 | “退出/保存直接复用首页自定义编辑器顶栏组件与按钮顺序。” |
| Marvin 标题旁不需要返回箭头 | 信息不足（明确后补充） | “全屏顶栏不在标题旁放返回箭头，退出动作放右侧操作区。” |
| 新建组件必须从零开始，不能默认完美数据 | 混合 | “列表可有演示卡；点击新建必须创建空项目，按导入 URDF → 添加 Mesh → 配置的流程渐进完成。” |
| 选中模型缺少箭头和原点 | 信息不足 | “3D 选中态必须同时显示局部高亮、原点和变换 Gizmo。” |
| 素材可删除、可添加到结构、可拖入 Link | 信息不足 | “素材库必须提供上传、删除、预览、添加、拖拽五类操作，并规定拖放目标。” |
| 结构树支持层级拖动和删除 | 信息不足 | “结构树必须支持 move/reparent/delete，并阻止非法循环和锁定节点操作。” |
| 型号模板、组件库、组件库2的 Button/Search/间距不统一 | 判断逻辑问题 | “先抽共享 list shell 和组件 Token，再开发第三页；不得完成后靠多个覆盖规则追平。” |
| 组件库2仍未遵循统一规范 | 判断逻辑问题 | “每次调整基础控件后，同时回归三个同级页面，不只修当前截图。” |
| 多处突然出现不该有的横线 | 判断逻辑问题 | “分隔线必须来自规范语义：页面无通栏线，卡片仅允许内缩分隔线；禁止全局 selector 扩散。” |
| 设计稿中标题分隔线应系统应用，不是只修圈出的一处 | 判断逻辑问题 | “修复规则时检索所有同类标题容器，按组件级规则一次性覆盖，而不是按截图坐标打补丁。” |
| 标题区图标按钮规格错误 | 判断逻辑问题 | “所有 header icon action 固定使用统一 IconButton size/variant，禁止裸 icon button。” |
| 素材库不应默认选中 | 信息不足偏少；更合理默认应为空 | “列表初始 selectedId 必须为 null，只有用户操作后才进入预览/选中状态。” |
| 结构树节点间距不足 | 判断逻辑问题 | “树行高、层级缩进、同级 gap 必须使用 Tree Token，不允许靠压缩容器凑空间。” |
| 工业风页面整体 UI 乱套、不统一 | 判断逻辑问题 | “工业风只通过 data-style + Token + 公共页面模板实现，禁止业务组件各写一套工业覆盖。” |
| 工业模式不应出现白色标题条 | 判断逻辑问题 | “工业列表标题区保持页面灰底和透明背景，不形成独立白色 Toolbar。” |
| 三个库外层必须一致 | 判断逻辑问题 | “型号模板、组件库、组件库2必须共用一个 ListPageShell；验收比较同一 viewport 下的 bounding box。” |
| 边距多次仍未统一 | 判断逻辑问题 | “不要只看声明值；检查嵌套容器、inline style、CSS specificity、固定高度和最终 computed style。” |
| 标题边距必须与字典配置、软件产品、装机记录一致 | 判断逻辑问题 | “以现有 `ds-page ds-page--list > ds-page-header` 为唯一基准，不重新定义近似值。” |
| 组件库和组件库2仍有额外边距 | 判断逻辑问题 | “检查业务特有 wrapper：组件库排除详情页 padding；组件库2移除 AdaptiveText 固定高度。” |

## 4. 当前问题

### 4.1 尚待用户视觉确认

1. **组件库与组件库2最新边距修复尚未得到用户确认。**
   - 组件库已增加更高优先级的 list selector，覆盖详情页工业 padding。
   - 组件库2已移除标题内部固定高度。
   - `npm run check:design-system` 和 `npm run build` 已通过，但仍需在用户实际窗口尺寸下截图比对。

2. **三个全屏详情/编辑页的顶栏仍需确认。**
   - 型号模板、组件库已新增工作区顶栏。
   - 组件库2保留自己的编辑顶栏。
   - 需确认按钮顺序、文案和高度是否完全符合首页自定义。

3. **本地浏览器自动视觉检查未完成。**
   - 浏览器工具因本地 URL 安全策略未能重新打开开发页。
   - 当前主要依赖代码审计、规范检查和构建；不能宣称已完成像素级视觉确认。

### 4.2 功能仍为原型级

- URDF 解析和导出覆盖了主要字段，但复杂 URDF、package URI、纹理路径和异常资源仍需强化。
- gRPC-Web 接口为基础原型，缺少真实环境鉴权、断线重连、错误分级和稳定性测试。
- 结构树拖动需继续补充循环依赖、Joint/Link 合法父子关系和锁定节点校验。
- 四元数/欧拉角切换需要补充边界值、万向锁和精度回归测试。
- Collision/Inertial 的场景辅助显示仍需逐项和 URDF 真实数据核对。
- 数据主要保存在 React 状态与 `localStorage`，尚无服务端持久化。

### 4.3 工程状态

- 工作区改动范围大，包含大量此前 UI 规范和业务页面修改。
- `public/models/`、Marvin 资源目录、`urdf-html-1.0-main/`、组件库2源码等仍为未跟踪文件。
- 不要为了整理本任务删除或重置其他用户改动。

## 5. 下一步计划

1. 在用户当前窗口下并排打开字典配置、型号模板、组件库、组件库2，记录标题和首张卡片的 `getBoundingClientRect()`。
2. 若仍有偏差，删除业务页专用 list spacing，而不是增加新的覆盖规则；最终收敛为单一 `ListPageShell` 组件。
3. 对三个详情/编辑页逐一验证：进入时导航与全局顶栏消失，返回时恢复，内容不跳动。
4. 按参考图检查全屏顶栏：高度、左右 padding、标题、状态、取消/退出、保存按钮顺序。
5. 用 Marvin URDF 做完整回归：Link/Joint 选择、关节运动、Mesh 拖入、Collision、Inertial、保存和重新打开。
6. 增加组件级测试：旋转换算、URDF 字段映射、结构树 move/reparent、素材删除引用清理。
7. 执行主题/尺寸矩阵：默认与工业、浅色与深色、1024/1440/1920 宽度。
8. 用户确认后再整理提交边界；未经确认不要提交或删除大范围文件。

## 6. 踩过的坑

1. **局部修截图，未先统一架构。** 多次按单个页面加 CSS，导致同级页面继续漂移。
2. **只检查声明值，不检查最终布局。** `padding:16px` 看似一致，但父层 gap、inline style、固定高度和 specificity 会继续叠加。
3. **把详情规则命中列表页。** `.robot-component-page` 的工业 padding 同时影响 list/detail，必须使用明确变体。
4. **固定文字高度破坏页头节奏。** AdaptiveText 的 30px/22px 高度让组件库2即使使用相同 padding 仍与标准页不齐。
5. **未优先复用现成规范和页面。** 项目已有 `ds-page-header`、设备结构树和首页全屏编辑机制，早期仍自行设计近似版本。
6. **误读布局层级。** “放在模型结构下面”被实现成结构树内部，正确语义是独立卡片位于其下方。
7. **把选中理解成全场景染色。** 正确做法是命中对象局部高亮、保持原材质并显示原点/Gizmo。
8. **为不明确功能主动造控件。** 无来源的工具切换、说明条、接口黑框和 HUD 增加了认知负担。
9. **示意模型不能替代真实验收。** 用户已经提供真实 URDF/网格时，应第一时间以真实模型验证材质、选中和关节联动。
10. **自动构建通过不等于视觉正确。** UI 任务必须补充实际窗口截图或 computed style/bounding box 比对。

## 7. 建议下次会话开头直接附加的总指令

```text
先不要凭经验新增样式或交互。开始编码前请先：
1. 阅读项目 UI 规范，并定位字典配置、软件产品、装机记录、设备结构树和首页自定义全屏编辑的现有实现；
2. 型号模板、组件库、组件库2必须复用同一个列表页外层、标题、搜索、按钮、卡片、选中态和间距 Token；
3. 所有 Button/Input/Tab/Card/Modal/IconButton 必须从 ProductUI 或项目已有组件复用，不允许手写第二套皮肤；
4. 进入任何型号/组件详情或编辑页时，统一隐藏左侧导航和全局顶栏，复用首页自定义的全屏工作区机制；
5. 不添加设计稿、规范或现有页面中没有的控件；不确定的功能先保持最小实现；
6. 3D 选中只高亮当前 Link/Joint，保留原材质，并显示原点和变换 Gizmo，禁止全模型染色或粗大包围盒；
7. 新建组件必须为空白流程，演示模型与新建状态分离；
8. 每次修改后同时回归三个同级页面，并用 computed style/getBoundingClientRect 对比，而不是只检查 CSS 声明；
9. 先完成视觉和交互对照检查，再运行设计规范检查与构建；未实际视觉验证时必须明确说明；
10. 保留工作区现有修改，不删除、不重置、不覆盖无关文件。
```

## 8. 验证命令

```bash
npm run check:design-system
npm run build
git diff --check
```

当前最近一次执行结果：设计规范检查通过，Vite 生产构建通过。构建仅有既存的大 chunk 警告。
