# 交互规范完整性审查报告

> 审查时间：2026-08-13 | 规范版本：1.7.0  
> 审查范围：`docs/ui-guidelines.md`、`components/component-specs.json`、`patterns/product-patterns.json`、`docs/page-recipes.md`、`docs/product-ui-mapping.md`、`docs/quick-reference.md`

---

## 总览

| 问题类型 | 数量 |
|---|---|
| 缺失的状态 / 边界场景 | 14 |
| 重复定义 / 冗余规则 | 9 |
| 规则冲突 | 7 |
| 可合并统一的逻辑 | 6 |
| 页面特殊规则应升为全局规范 | 4 |
| **合计** | **40** |

---

## 一、缺失的状态与边界场景

### 1.1 Input / TextArea 缺少 `Loading` 状态描述

**位置**：第 13 节组件契约表（ui-guidelines.md L329）、component-specs.json Input 条目  
**现状**：Input 契约中列出了 `Default / Hover / Focus / Filled / Readonly / Disabled / Error`，**没有 `Loading` 状态**，但第 14.3 节的 FormItem 状态矩阵中有 `Loading`（异步候选、上传或校验显示局部进度）。  
**风险**：开发无法知道异步校验时 Input 应呈现什么样式（Spinner 位置、输入禁用与否、宽度保持）。

**补充规则**：
> Input / TextArea 的 `Loading` 状态：在字段右侧尾部显示 16px Spinner，输入保持可用但校验行为挂起；控件宽度和高度不得因 Loading 改变；Loading 结束后恢复 Default 或直接进入 Error。

---

### 1.2 Select 缺少 `Filled`（已选中有值）的单独视觉定义

**位置**：component-specs.json Select 条目、第 13.3 节  
**现状**：Select 列出了 `Default / Hover / Pressed / Focus / Open / Selected / Disabled / Loading / Error`，但没有明确 `Filled`（即有选中值但非 Open 状态）的视觉规则，开发可能把它复用为 Default，导致有值与无值外观一致。  
**补充规则**：
> Select 的 `Filled` 状态：已选中值时，占位文字替换为选中 Label（使用 `--ds-color-heading` 或 `--ds-color-text`）；有清除入口时显示清除 IconButton；触发器外观与 Default 相同，不变色。

---

### 1.3 Checkbox 组缺少"部分选中（Indeterminate）"状态

**位置**：第 13.4 节 Checkbox、component-specs.json、第 15.3 节全选  
**现状**：全选的"不确定态"在第 27.26 节提到"以不确定态表示部分选中"，但 Checkbox 组件契约中无此状态，Token 也未列出。  
**补充规则**：
> Checkbox 的 `Indeterminate` 状态：使用 `--ds-checkbox-indeterminate-bg` 和水平短线图标（非勾选图标）表达父级/全选 Checkbox 的部分选中；可与 Focus 同时存在；勾选全部后恢复 Checked，取消全选后恢复 Default。

---

### 1.4 Switch 缺少 `Loading` 状态规则（只有叙述）

**位置**：第 13.4 节 Switch  
**现状**：第 13.4 节说"切换后如需要异步保存，应在控件附近反馈"，但 Switch **没有在状态表中列出 `Loading`**，也没有说 Spinner 在哪里显示、开关滑块是否锁定、如何阻止重复切换。  
**补充规则**：
> Switch 的 `Loading` 状态：切换后立即锁定开关，在开关右侧或内部显示 Spinner；Loading 期间阻止再次切换；成功后更新状态，失败时恢复原值并显示错误提示（字段附近，不弹 Toast）。

---

### 1.5 Tag 缺少"可移除（Closable）"变体的状态规则

**位置**：component-specs.json Tag 条目、第 3.1 节  
**现状**：规范多处提到"标签增删"场景（L79"标签增删等紧凑控件保持 24–32px"），但 Tag 契约只有 `Default / Semantic tone` 两种状态，没有带关闭按钮的 Closable Tag 的 Hover（出现 × 图标）/ 点击移除的视觉规则。  
**补充规则**：
> Closable Tag 的交互规则：Tag Hover 时右侧出现 × IconButton（16px 热区至少 24×24px）；点击 × 触发移除逻辑，不触发 Tag 自身的点击事件；移除无需二次确认；键盘 Tab 可聚焦到 × 按钮并按 Delete / Backspace 移除。

---

### 1.6 Pagination 缺少"结果数为 0" 和"仅 1 页"时的行为

**位置**：第 13.5 节 Pagination、component-specs.json Pagination 条目  
**现状**：规范只说"前后页不可用时 Disabled"，但没有规定：结果为 0 时是否显示 Pagination、仅 1 页时是否显示。  
**补充规则**：
> 结果为 0 时不显示 Pagination，由空状态替代。仅 1 页时隐藏 Pagination 或保留但隐藏页码列表，仅保留总数文案（如"共 5 条"），不显示不可交互的单个页码。

---

### 1.7 Table 缺少"行展开后，子内容本身的 Loading / Error"处理

**位置**：第 15.2 节展开行、component-specs.json Table 条目  
**现状**：表格的"展开行"只说"用 `aria-expanded` 标识，展开内容与主行关联，缩回时恢复原行高度"，没有覆盖展开后子内容懒加载（Loading）或请求失败（Error）的状态。  
**补充规则**：
> 行展开的子内容状态：展开后子区域使用固定最小高度的 Skeleton 或局部 Spinner；请求失败时在子区域内显示简短说明与重试按钮，不折叠父行，也不显示整行红色背景。

---

### 1.8 Drawer 缺少"自身内容加载失败"的处理

**位置**：第 20.2 节抽屉详情模板、page-recipes.md 第 4 节  
**现状**：第 20.2 节说"加载中保留 Drawer 壳；对象删除、无权限或接口失败均在 Body 内说明并提供可行去向"，但 page-recipes.md 没有对应的具体处理方式，组件契约也没有。  
**补充规则**：已在 20.2 节有叙述，但 **page-recipes.md 第 4 节 "Drawer 详情"应补充**：
> Drawer Body 内容加载失败时：保留 Drawer 壳，Body 居中显示错误说明（不显示接口原始报错）和"重试"按钮；对象已被删除时说明"内容已删除"并提供关闭入口；Footer 关闭按钮始终可用。

---

### 1.9 SearchInput 缺少"防抖期间"的视觉状态

**位置**：第 13.2 节 SearchInput  
**现状**：规范说"Loading 在输入框尾部展示，保留已输入关键词与控件宽度"，但没有说明**防抖期间**（用户已输入但请求尚未发出）的视觉：是否显示 Spinner？还是什么都不显示？开发会不一致。  
**补充规则**：
> SearchInput 防抖期间不显示 Loading 指示；**只有请求已发出且尚未返回时**才在尾部显示 Spinner；请求完成后 Spinner 消失，结果立即更新。防抖时长默认 200–300ms，不得在 composition 期间触发请求。

---

### 1.10 Modal / Drawer 的"多步"内容切换缺少步骤状态规则

**位置**：第 3.2 节弹窗标准、第 27.11 节、page-recipes.md 第 6 节导入流程  
**现状**：规范说"不在 Modal 中再打开 Modal；后续任务改为同一 Modal 内切步"，但对于切步 Modal 的步骤指示器、如何处理"上一步回显"和"下一步校验失败"没有统一规则。  
**补充规则**：
> 多步 Modal 内的步骤切换：步骤指示器放在 Header 或 Body 顶部（不在 Footer）；向上一步时保留当前步骤已填内容；向下一步时先校验当前步骤，失败时聚焦第一个无效字段并阻止跳转；步骤指示器使用已发布 Steps 契约，不自行设计进度 UI。

---

### 1.11 空状态（Empty）缺少"无创建权限"时的具体入口规则

**位置**：第 27.13 节 Empty、第 20.4 节页面级状态矩阵  
**现状**：两处都提到"有创建权限时提供创建入口；否则不提供无效按钮"，但没有说明"无创建权限"时应该显示什么替代内容（仅说明？提供申请权限链接？）。  
**补充规则**：
> 无数据且无创建权限时：空状态提供"为什么为空"的说明 + 可选的"联系管理员"或"申请权限"文字链接（非 Button）；不显示灰色禁用 Primary 按钮；不提供任何会触发错误的操作入口。

---

### 1.12 Toast / Notification 缺少"操作撤销（Undo）"变体的规则

**位置**：第 16 节浮层与反馈规范、第 27.10 节删除规范  
**现状**：第 27.10 节说"可撤销的单项低风险删除可先执行并提供有时限的 Undo"，但 Toast / Notification 的规范中没有 Undo 操作样式、时限显示、倒计时进度条等规则。  
**补充规则**：
> Undo Toast：最小高度 `--ds-toast-two-line-min-height`（64px）；操作区使用 32px Soft/Flat 语义 Button（如"撤销"）；倒计时由进度条（Toast 底部 2px 线）或文案（"3 秒后删除"）表达，二者取其一且全局统一；超时后 Toast 自动关闭，不可再撤销；撤销后 Toast 立即关闭并在原位置恢复对象。

---

### 1.13 缺少"通用 Readonly 文本字段"的复制行为规则

**位置**：第 13.3 节 Input Readonly、第 12.1 节状态模型  
**现状**：规范说"Readonly 保留正常文本对比度并允许复制"，但没有规定：是否提供一键复制 IconButton？是否有复制成功的反馈？  
**补充规则**：
> Readonly 字段默认允许用户自行选中并复制，不强制提供复制 IconButton。若业务需要一键复制（如 API Key、编号），则在字段尾部放 16px CopyIcon IconButton；点击后触发 `navigator.clipboard.writeText`，成功后 Icon 临时切换为对勾图标（1.5 秒）或显示短暂 Tooltip "已复制"；失败时静默回退，不弹错误 Toast。

---

### 1.14 缺少"Skeleton 骨架屏"最大等待时间与超时降级

**位置**：第 20.4 节、第 27.9 节、第 27.13 节  
**现状**：多处提到使用 Skeleton 进行首次加载，但没有规定 Skeleton 显示的最大时间、超时后应该显示什么（Error 状态还是继续等待）。  
**补充规则**：
> Skeleton 超时处理：若数据在 15 秒内未返回，自动降级为 Error 状态并提供重试；不允许无限 Skeleton 掩盖超时或失败。具体超时阈值由页面声明，未声明时使用 15 秒默认值。超时前不向用户显示"等待中"文案，超时后说明加载时间过长及可选重试。

---

## 二、重复定义 / 冗余规则

### 2.1 弹窗关闭规则四处重复

**位置**：L120（第 3.2 节）、L226（第 11.2 节）、L986（第 27.3 节）、L1061（第 27.11 节）  
**现状**：
- L120："右上角 X、Esc、点击遮罩及 Footer '关闭/取消'必须使用同一套关闭逻辑"
- L226："弹窗通过右上角关闭、Esc、点击遮罩、Footer 的'关闭/取消'四种入口退出"
- L986："Esc 关闭当前最上层可关闭浮层"
- L1061："关闭按钮、Esc、遮罩和路由离开都进入同一确认逻辑"

**处理建议**：在第 3.2 节保留权威定义，其他各节改为"见第 3.2 节弹窗关闭规则"。

---

### 2.2 "同一操作区只允许一个 Primary"五处表述

**位置**：L76（第 3 节）、L116（第 3.2 节）、L175（quick-reference.md）、L343（第 13.1.1 节）、L634（第 20.1 节）  
**处理建议**：在第 13.1.1 节 Button 使用边界保留权威定义（含同一操作区范围说明），其他地方缩减为一句"同一操作区最多一个 Primary（见第 13.1.1 节）"。

---

### 2.3 "搜索/筛选实时生效，不放查询按钮"四处重复

**位置**：L350（quick-reference.md 第十一节）、L384（第 13.2 节 SearchBar）、L493（第 15.2 节 Table 筛选）、L1021（第 27.7 节即时筛选）、page-recipes.md L31  
**处理建议**：在第 13.2 节 SearchBar 保留完整规则，其余处改为"见第 13.2 节搜索与筛选规则"。

---

### 2.4 Disabled 透明度在三处重复定义

**位置**：L60（第 1 节 Token 说明）、L220（第 11.1 节）、L304（第 12.2 节）  
**处理建议**：Token 值的权威来源在 L60（"Disabled 透明度统一为 0.45"），另两处改为"引用 `--ds-opacity-disabled`"即可，不重复数值。

---

### 2.5 Focus Ring 的 2px 规格在四处重复

**位置**：L151（第 7 节）、L319（第 12.3 节）、L354（第 13.1.2 节状态矩阵）、L992（第 27.4 节）  
**处理建议**：在第 12.3 节"基础状态行为"保留权威规则，其余引用第 12.3 节。

---

### 2.6 Modal 宽度四档标准三处重复列表

**位置**：L100（第 3.2 节表格）、L206（quick-reference.md 第六节）、component-specs.json Modal 条目  
**处理建议**：以第 3.2 节为权威来源，quick-reference 中仅保留示例代码，JSON 契约引用规范。

---

### 2.7 图标尺寸五档规则两处完整重复

**位置**：L246（第 11.5 节）和 component-specs.json 各组件的 `metrics` 字段中均有图标尺寸  
**处理建议**：以第 11.5 节为图标尺寸权威来源，组件契约中只写该尺寸档对应的 Token 引用，不重复数字。

---

### 2.8 弹窗 Body 唯一滚动与 Header/Footer 固定在六处重复

**位置**：L115（第 3.2 节）、L523（第 16 节 Modal 条目）、L693（第 20.3.1 节）、L730（第 20.5 节）、L1060（第 27.11 节）、page-recipes.md L43  
**处理建议**：在第 3.2 节保留一次完整说明，其他处替换为"见第 3.2 节滚动规则"。

---

### 2.9 快捷键"不拦截系统保留键"两处内容相同

**位置**：L988（第 27.3 节）和 L1289（第 27.34 节）  
**处理建议**：在第 27.34 节快捷键体系保留完整内容，第 27.3 节删除重复项，改为"见第 27.34 节快捷键体系"。

---

## 三、规则冲突

### 3.1 Modal 的 Header/Footer 高度有歧义

**文档 A**（L523，第 16 节）：`"Header/Footer 固定且统一为 64px"`  
**文档 B**（第 3.2 节）：未提到 64px，只说"Header 左右间距 24px；Body 左右间距 24px；Footer 右对齐"  
**文档 C**（component-specs.json Modal 条目）：未列出高度  

**裁决**：Modal Header 的高度应由标题（18px/600）+ 上下内边距决定，不是固定 64px（因为弹窗描述信息的有无会影响高度）。**第 16 节的 64px 表述有误，应删除 "64px" 并改为**：
> Modal Header 高度由标题文字（18px/600）加上下内边距（24px 上，20px 下）决定；当有描述文案时高度自然增加；不设固定 64px 高度约束。

---

### 3.2 Industrial 密度的表头/数据行高度与全局规则冲突

**文档 A**（L791，第 22.2 节 Industrial）：`"表头 40px、数据行 48px"`  
**文档 B**（L78 / L475 / L821，全局基础组件规范）：`"表头 44px，数据行 60px"`  
**文档 C**（component-specs.json Table 条目）：列出 `headerHeight: 44, rowHeight: 60`（Current 值）  

**裁决**：Industrial 预设使用不同密度是合理的，但规范必须明确两套数值的适用范围：
> Current（默认）预设：表头 44px，数据行 60px。  
> Industrial 预设：表头 40px，数据行 48px。  
> component-specs.json 应新增 `industrial` 对应的 `headerHeight / rowHeight` 字段，不得让同一 Token 覆盖两种预设。

---

### 3.3 按钮尺寸描述与快速速查表存在微差

**文档 A**（L80，第 3 节）：`"常规文字按钮默认 40px 高，紧凑按钮只允许 32px，迷你图标操作只允许 24px"`  
**文档 B**（quick-reference.md L157–161）：按钮尺寸表中的"xs（24px）""sm（32px）""md（40px）"与上文一致，但描述"sm：紧凑工具栏、筛选区次要操作"——筛选区控件究竟应该是 32px 还是 40px？（第 3 节说"搜索框与'导入、导出、刷新、新建/新增'统一 40px 高"，筛选 Select 没有明确）  

**裁决**：
> 工具栏中的搜索框、筛选 Select、主要操作按钮统一 40px；只有在工具栏空间明确不足时，次要筛选控件可降为 32px 且必须全行统一，不得混用 40px 和 32px 控件在同一行。

---

### 3.4 Tag 是否可有 Hover 状态存在矛盾

**文档 A**（L89，第 3.1 节）：`"Tag 不带描边、不承担操作"`（隐含无 Hover）  
**文档 B**（第 1.5 章 Closable Tag 缺失说明中引申）：可移除的 Tag 需要 Hover 出现 × 按钮（这意味着有 Hover 交互）  
**文档 C**（component-specs.json Tag）：`states: ["Default", "Semantic tone"]`，无 Hover  

**裁决**：
> 普通（只读）Tag：不提供 Hover 状态，不可点击，不承担操作语义。  
> Closable Tag：整体 Tag 不提供 Hover 高亮；只有 Tag 内部的 × IconButton 有 Hover / Pressed / Focus 状态。Tag 本体不可被整体聚焦，× 按钮独立进入 Tab 序列。

---

### 3.5 表单 Modal 中字段间距规则存在冲突

**文档 A**（第 20.3.1 节，L694）：`"同一行字段间距 16px，字段组间距 16px"`  
**文档 B**（page-recipes.md L40）：`layout: { fieldGap: 20, sectionGap: 24 }`（product-patterns.json）  
**文档 C**（第 14.1 节表单规范）：`"常规字段间距 16px；表单分组间距 24px"`  

**裁决**：以第 14.1 节为权威定义：
> 字段间距（控件到下一个控件）：16px；分组间距（一组字段到下一组 Section 标题）：24px。  
> product-patterns.json 中的 `fieldGap: 20` 有误，应修正为 16。第 20.3.1 节与第 14.1 节一致，以第 14.1 节为准。

---

### 3.6 "状态回退使用品牌主按钮"与"危险操作使用 danger" 的边界模糊

**文档 A**（L117，第 3.2 节）：`"取消发布、停用等可恢复的状态回退使用品牌主按钮"`  
**文档 B**（L668，第 20.3 节状态回退确认）：`"可恢复的回退使用品牌主按钮，不可逆或有数据损失风险时才使用 Danger"`  
**文档 C**（第 27.10 节）：`"不可逆或高影响操作必须使用 Danger 语义"`——没有提到"可恢复的回退用主按钮"的特例  

**裁决**：以第 20.3 节为权威，第 27.10 节补充该例外：
> 操作可恢复（对象仍可重新发布、重新启用）→ 品牌主按钮。  
> 操作不可逆（数据永久删除或生产环境状态翻转无法回滚）→ Danger 按钮。  
> 判断依据：业务是否能在不丢失数据的情况下撤销，而非"停用"或"发布"本身的字眼。

---

### 3.7 导航与历史状态有两节内容高度重叠且有冲突

**文档 A**（第 27.22 节导航与返回逻辑，L1176–1183）  
**文档 B**（第 27.32 节导航与历史状态，L1266–1273）  
两节均包含"返回列表保留筛选/分页/选择"、"浏览器返回与业务返回一致"，文本高度相似，但：  
- 27.22 说"`Tabs` 若表示可直达子视图则写入 URL；只表示局部瞬时视图时可留在本地状态"  
- 27.32 说"Tab 切换保留各自状态（筛选、滚动、未提交输入）"  
——当 Tab 是本地状态时，"保留各自状态"与"不写入后退栈"如何同时满足未说清楚。

**裁决**：合并两节，以 27.32 为标题（"导航、历史状态与路由"），并明确：
> 本地状态 Tab 的各项状态（筛选、滚动）保留在内存中，切换回该 Tab 时恢复，但这些状态不写入浏览器历史，用户点击浏览器后退不会回到上一个 Tab。若业务需要 Tab 可深链接，必须写入 URL 参数。

---

## 四、可合并统一的逻辑

### 4.1 "浮层关闭行为"统一规则

当前分散在：第 3.2 节（Modal）、第 11.2 节（弹窗与确认）、第 16 节（浮层表）、第 27.11 节（Dropdown / Popover / Modal / Drawer）。  
建议在第 16 节浮层规范开头增加"统一关闭行为"子节：

> **统一关闭行为**（适用所有浮层）：  
> 1. 所有浮层支持以下任一方式关闭：触发器再次点击 / × 按钮 / Esc / 点击遮罩（危险或有未保存内容时除外）/ Footer "取消/关闭"。  
> 2. 含未保存内容的表单型浮层（Modal / Drawer）：Esc / × / 遮罩 → 先进入确认对话框，取消确认不修改数据。  
> 3. Dropdown / Popover：点击外部和 Esc 直接关闭，不进入确认。  
> 4. 关闭后焦点统一返回打开该浮层的触发元素；若触发元素已销毁，回到同级容器标题。  
> 各节不再重复，仅引用本条。

---

### 4.2 "异步操作的 Loading、防重入、反馈"统一规则

第 11.1 节、第 13.1 节 Button、第 14.2 节表单提交、第 27.9 节异步操作均有类似规则。  
建议将以下内容提升为独立的"**异步操作通用契约**"（放在第 27.9 节或独立的 27.0 节），各处引用：

> **异步操作通用契约**：  
> - 触发元素进入 Loading，阻止重复提交，保持宽度和位置不变。  
> - 100ms 内完成：不显示 Loading；300ms 以上：显示局部 Spinner 或进度；10s 以上：显示阶段说明与后台入口。  
> - 成功：说明结果与影响对象；失败：保留用户输入和上下文，说明原因，给出可执行恢复路径。  
> - 过期请求（网络乱序）使用序号或请求 ID 丢弃，不覆盖新结果。

---

### 4.3 "错误状态必须提供恢复路径"统一规则

分散在：第 7 节（Error 说明问题与恢复方式）、第 15.2 节（Error 说明失败原因）、第 27.13 节（Error 说明范围和可执行动作）、第 27.48 节（逃生路径）。  
建议提炼统一规则置于第 12 节组件状态模型下：

> **Error 状态最低要求**：① 用可理解的语言说明发生了什么（不显示 HTTP 状态码或内部报错）；② 说明影响范围；③ 提供至少一个可执行动作（重试 / 返回 / 申请权限）；④ 错误信息使用 `role="alert"` 或 `aria-live="assertive"` 播报；⑤ 错误状态图标必须配合错误文字，不只依赖红色。

---

### 4.4 "未保存内容离开保护"统一规则

分散在：第 3.2 节（未保存表单关闭前需二次确认）、第 14.2 节（关闭含未保存修改的表单时必须请求确认）、第 27.11 节（含未保存修改时 Esc/遮罩/路由离开都进入确认逻辑）、第 27.17 节（路由离开/切换对象提供三选）、第 27.36 节（Modal 被意外关闭保留草稿）。  
建议在第 27.17 节（Undo/Redo 与未保存状态）增加权威定义，其余引用：

> **未保存离开保护统一契约**：  
> - 表单字段、弹窗内容、行内编辑有任何脏值时，以下行为都触发保护：× 关闭、Esc、遮罩点击、浏览器后退、路由导航、关闭浏览器标签。  
> - 保护对话框提供：「保存并离开」/ 「放弃修改」/ 「继续编辑」三个选项（仅关闭弹窗时可简化为「放弃修改」/ 「继续编辑」）。  
> - 取消保护操作（"继续编辑"）不修改数据；放弃修改后不可恢复且不再提示。  
> - 自动保存（草稿）不替代保护：用户主动"放弃"才清除草稿，页面刷新恢复草稿需用户确认。

---

### 4.5 "权限不足"在多个级别重复定义

分散在：第 20.3 节"权限不足"业务组合、第 20.4 节"无权限"页面状态矩阵、第 27.21 节权限、占用与不可用状态、第 27.33 节路由与深链接（无权限时路径）。  
建议将"权限"相关的边界规则统一在第 27.21 节，第 20.3 节 / 20.4 节只保留简要定义和引用：

> 权限状态分四类：①不可见（隐藏入口）；②可见但 Disabled（需解释原因）；③被占用（显示占用者与接管入口）；④依赖条件未满足（显示条件说明）。无论哪类，均不得显示会报错的操作入口，也不得把所有权限问题统一为"无权限"提示。

---

### 4.6 "状态只依赖颜色"的禁止规则四处重复

**位置**：L127（第 4 节）、L155（第 7 节）、L320（第 12.3 节）、L1101（第 27.13 节）  
建议在第 12.1 节状态命名表末尾增加一条全局约束，其余处删除重复表述：

> **通用约束（全局）**：所有状态（错误、成功、选中、禁用、告警）不得仅依赖颜色传达，必须配合文字、图标、形状或 ARIA 属性中的至少一种。

---

## 五、页面特殊规则应升为全局规范

### 5.1 Custom Dashboard 编辑器的层级规则应明确边界

**位置**：第 20.2 节末尾（L656）："编辑器画布的局部图层是唯一允许使用数值层级的场景"  
**现状**：这条规则隐藏在页面模板矩阵末尾，并非全局规范。但它实际上定义了一个重要的例外，应在第 16 节浮层规范的层级表旁边单独标注：

> **画布图层例外**：全屏编辑器（`.ds-page--editor`）的画布内部元素可以使用业务数值层级，但层级作用域必须限制在画布容器内（通过 `isolation: isolate` 或父容器新建层叠上下文），不得越出画布影响页面全局浮层。

---

### 5.2 列表→详情的查询上下文保留规则应升为全局规范

**位置**：product-patterns.json `preserveListContext`（query / filters / sorting / pagination / scrollPosition）、page-recipes.md L55  
**现状**：这条规则只在 Drawer 详情场景中定义，但在"列表→整页详情→返回列表"场景中同样适用，且第 27.22 / 27.32 节都提到但不够系统。  
**建议**：在第 27.22 节加入系统化的"列表上下文保留"规则：

> 从列表页打开详情（无论 Drawer、整页还是弹窗）再返回时，以下列表状态必须完整保留：搜索关键词、已生效筛选条件、当前排序、分页页码、多选选择集、滚动位置、已展开的行（如有）。若对象已被删除，对应行移除并将焦点移到相邻行，其余状态不受影响。

---

### 5.3 "分组标题 / Section 标题"的样式规则应从页面 Recipe 升为全局规范

**位置**：page-recipes.md L44："分组标题 14px / 600"  
**现状**：第 1.1 节字体规范中只说"模块标题 16px / 600"，没有覆盖弹窗 / 表单内部的 Section 分组标题。page-recipes 定义了 14px/600，与全局字体规范不完全对应。  
**补充到第 14.1 节**（表单字段结构）：

> 表单内部 Section 分组标题：14px / 600 / `--ds-color-heading`，与正文字号相同但字重加粗以区分层级；不使用 16px（16px 是模块级别标题，用于卡片外部）。分组标题下方使用 `--ds-color-border` 1px 分割线（仅 Body 内部分区需要时）。

---

### 5.4 "宽表操作列宽度"规则只在 Recipe 定义

**位置**：page-recipes.md L34："操作列默认固定右侧，基准宽度 96px；操作组使用 32px ProductIconButton，间距 8px"  
**现状**：第 15 节 Table 规范中提到"操作列固定在右侧"但没有给出宽度。  
**补充到第 15.1 节**（表格结构）：

> 操作列默认固定在右侧，基准宽度 96px（容纳 3 个 32px IconButton + 8px 间距）；如业务操作超过 3 个，多余操作收进 Dropdown"更多"；操作列宽度不因语言扩展，IconButton 依赖 `aria-label` 和 Tooltip 而非文字。

---

## 六、修订清单（按优先级）

### P0 必须立即修正（存在规则冲突）

| # | 位置 | 问题 | 动作 |
|---|---|---|---|
| 1 | 第 16 节 L523 | Modal Header/Footer "64px" 说法有误 | 删除 64px，改为内边距说明 |
| 2 | product-patterns.json L40 | fieldGap: 20 与第 14.1 节 16px 冲突 | 修正为 16 |
| 3 | 第 22.2 节 Industrial 密度 | 表头 40px / 数据行 48px 未与全局区分 | 在 component-specs.json 补充 Industrial 行高字段 |
| 4 | 第 27.22 / 27.32 节 | 两节内容高度重叠且 Tab 状态保留有歧义 | 合并两节，明确本地 Tab 状态与 URL 的关系 |

### P1 应在下次迭代补充（缺失状态）

| # | 组件 / 场景 | 缺失内容 | 补充位置 |
|---|---|---|---|
| 5 | Input | Loading 状态（异步校验） | 第 13.3 节 + component-specs.json |
| 6 | Select | Filled 状态视觉定义 | 第 13.3 节 |
| 7 | Checkbox | Indeterminate 状态 | 第 13.4 节 + component-specs.json |
| 8 | Switch | Loading 状态规则 | 第 13.4 节 |
| 9 | Tag | Closable 变体交互规则 | 第 3.1 节 + component-specs.json |
| 10 | Pagination | 0 条 / 1 页时的行为 | 第 13.5 节 |
| 11 | Table | 展开行子内容 Loading / Error | 第 15.2 节 |
| 12 | Drawer | 自身内容加载失败 | page-recipes.md 第 4 节 |
| 13 | SearchInput | 防抖期间 Loading 规则 | 第 13.2 节 |
| 14 | Empty | 无创建权限时的替代入口 | 第 27.13 节 |
| 15 | Toast | Undo 变体规则 | 第 16 节 |
| 16 | Readonly 字段 | 一键复制行为 | 第 13.3 节 |
| 17 | Skeleton | 超时降级规则 | 第 27.9 节 |
| 18 | Modal 多步 | 步骤切换状态规则 | 第 27.11 节 |

### P2 规范整合（减少重复）

| # | 重复内容 | 权威位置 | 其他位置处理 |
|---|---|---|---|
| 19 | 弹窗关闭规则 | 第 3.2 节 → 升为"统一关闭行为"到第 16 节 | 其余处引用 |
| 20 | 单 Primary 规则 | 第 13.1.1 节 | 其余处缩短为一句引用 |
| 21 | 实时搜索规则 | 第 13.2 节 | 其余处引用 |
| 22 | Disabled 透明度 | 第 1 节 Token | 其余处只写 Token 名 |
| 23 | Focus Ring 2px | 第 12.3 节 | 其余处引用 |
| 24 | Modal 宽度四档 | 第 3.2 节 | quick-reference 保留代码示例，JSON 引用规范 |
| 25 | 弹窗 Body 唯一滚动 | 第 3.2 节 | 其余处引用 |
| 26 | 错误依赖颜色禁止 | 第 12.1 节末尾新增全局约束 | 其余处删除重复 |
| 27 | 未保存离开保护 | 第 27.17 节（扩充为权威定义） | 其余处引用 |

---

## 七、已验证的正确规范（无需改动）

以下内容经检查一致、完整，无需修改：

- Design Token 命名体系与禁止硬编码规则（第 1 节）
- 按钮三档高度（24 / 32 / 40px）和圆角（8px）
- 页面布局响应断点和边距值
- 状态模型命名（第 12.1 节，Active 禁用已明确）
- 图标规范（第 11.5 节，五档尺寸、viewBox、currentColor）
- 多语言规范（第 26 节，完整）
- Industrial 风格与 Current 的独立作用域规则
- 无障碍规范（第 24 节，WCAG 2.2 AA 目标）
- 动效 Token 与场景矩阵（第 21 节）

---

*本报告基于 2026-08-13 版本规范审查，所有修订建议须经设计与前端双方评审后生效。*
